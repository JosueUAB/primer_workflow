name: Use Secrets
on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Use API Key
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "API Key length: ${#API_KEY}"
          # La key NO se imprime en logs, solo su longitud
      - name: intentar imprimir api key
        env:
            API_KEY: ${{secrets.API-KEY}}
        run: |
          echo "imprimir directamete la api key : "
          echo "la api key es : ${{ secrets.API_KEY }} "
          echo "la api key como variable es: $API_KEY"
      - name: Simulate deployment
        env:
          API_KEY: ${{ secrets.API_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          echo "Deploying with credentials..."
          echo "Connected to database"
          # Los valores reales están ocultos/vacíos por ahora